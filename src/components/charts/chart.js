import React, { useState } from 'react';
import { max, min } from "d3";


//fetch the asynchronous data later on 

export function Chart(props){
    return(
        <>
            <svg viewBox="0 0 {props.width} {props.height}">
            </svg>
            <p>/testing data/i</p>
        </>
        
    );
}

export default Chart;
